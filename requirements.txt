streamlit>=1.34
pandas>=2.2.2
numpy>=1.26.4
openpyxl>=3.1.2
# reportlab dependency has been retained for backward compatibility with
# previous versions of the PDF generator.  The new export function uses
# matplotlib, so we explicitly add it below.
reportlab>=3.6.13
matplotlib>=3.6